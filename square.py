#define a function sumOfSeries for input n
def sumOfSeries(n):
  sum=0
  
 #iteration loop to square each number and add them to get the square
 for i in range(1, n+1):
    sum = sum + (i*i)
    
    #returns the resulting square of a number input
    return num
  
  #sample input
  n=2
  print(sumOfSeries(n))
