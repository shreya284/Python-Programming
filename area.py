#define the main function findArea for radius r to find the circle's area
def findArea(r):
    PI = 3.142
    
    #calculates the area of the circle by taking the value of r(radius)
    return PI*r*r
  
  #prints the result of the area of the circle
  print("Area is: %.6f", findArea(n));
